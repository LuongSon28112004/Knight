using UnityEngine;

public static class StaticConst
{
    /// <summary>
    /// The minimum and maximum values for decibels (dB).
    /// These values are used to define the range of decibel levels in the audio system.
    /// </summary>
    public const float MIN_DB = -80.0f;
    public const float MAX_DB = 10.0f;

    /// <summary>
    /// The minimum and maximum values for volume.  
    /// These values are used to define the range of volume levels in the audio system.
    /// </summary>
    public const int MAX_COIN = 10;
    public const int MIN_COIN = 1;
    public const int MAX_HEALTH = 2;
    public const int MIN_HEALTH = 1;
}
